// src/pages/LoginPage.jsx
import LoginPage from "../components/Login"; // Adjust the import path if necessary

const Login = () => {
  return (
    <div>
      <LoginPage />
    </div>
  );
};

export default Login;
